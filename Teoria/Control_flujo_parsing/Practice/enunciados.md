### Ejercicios para practicar (sin solución)
* Implementa una función `ft_putstr` que imprima una cadena y actualice un contador.
* Modifica un `ft_printf` mínimo para que soporte `%c`, `%s`, y `%%`.
* Escribe la lógica de parsing que detecte un `%` y llame a la función adecuada.
* Crea una función `handle_specifier` con un switch en vez de if/else.
* Haz que tu `ft_printf` retorne el número total de caracteres impresos.
* Añade soporte para `%p` usando una función `ft_putptr`.